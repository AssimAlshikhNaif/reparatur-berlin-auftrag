services:
  backend:
    build: ./backend
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/backend/uploads
    restart: always

  frontend:
    build: ./frontend
    restart: always
    ports:
      - "3000:80"
    depends_on:
      - backend
